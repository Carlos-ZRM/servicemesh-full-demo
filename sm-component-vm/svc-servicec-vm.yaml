apiVersion: v1
kind: Service
metadata:
  name: svc-servicec-vm
spec:
  internalTrafficPolicy: Cluster
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    kubevirt.io/domain: servicec-vm
  sessionAffinity: None
  type: ClusterIP